{
  "name": "regex-tab-keeper",
  "version": "1.2.0",
  "private": true,
  "description": "A Manifest V3 Chrome extension that keeps tabs matching URL regular expressions.",
  "scripts": {
    "check": "node --check core.js && node --check background.js && node --check popup.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
